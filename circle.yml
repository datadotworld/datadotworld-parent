machine:
  java:
    version: openjdk8

  environment:
    _JAVA_OPTIONS: "-Xms512m -Xmx1792m"

deployment:
  releases:
    branch: master
    commands:
      - mvn deploy -s settings.xml
      - ./mvn-release.sh
